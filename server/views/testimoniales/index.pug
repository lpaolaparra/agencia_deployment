extends ../layout/index
block contenido 
    main.container.mt-5
        h1.text-center.mb-5 #{pagina}

        .row
            .col-md-12
                include ../layout/partials/testimoniales
            .col-md-12.mt-5
                h2.text-center Agrega un Testimonial
                //- Revisamos que halla errores
                if(errores)
                    each error in errores
                        .alert.alert-danger.text-center=error.mensaje
                .row.justify-content-center
                    .col-md-8
                        form(action="/testimoniales", method="POST")
                            .form-group
                                label(for="nombre") Nombre
                                input(type="text", class="form-control" placeholder="Tu Nombre" name="nombre" id="nombre" value=nombre)
                            .form-group
                                label(for="correo") Correo Electronico
                                input(type="text", class="form-control" placeholder="Tu Correo Electronico" name="correo" id="correo" value=correo)
                            .form-group
                                label(for="mensaje") Mensaje
                                textarea#mensaje( rows="3" class="form-control" placeholder="Tu Mensaje" name="mensaje")=mensaje
                            input.btn.btn-success.btn-block(type="submit", value="Agregar")
